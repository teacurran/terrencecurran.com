diff --git a/node_modules/emdash/src/api/schemas/media.ts b/node_modules/emdash/src/api/schemas/media.ts
index ca3f320..bbaf74c 100644
--- a/node_modules/emdash/src/api/schemas/media.ts
+++ b/node_modules/emdash/src/api/schemas/media.ts
@@ -27,7 +27,7 @@ const MAX_UPLOAD_SIZE = 50 * 1024 * 1024;
 export const mediaUploadUrlBody = z
 	.object({
 		filename: z.string().min(1, "filename is required"),
-		contentType: z.string().min(1, "contentType is required"),
+		contentType: z.string().default("application/octet-stream"),
 		size: z
 			.number()
 			.int()
diff --git a/node_modules/emdash/src/astro/routes/api/media.ts b/node_modules/emdash/src/astro/routes/api/media.ts
index 31f146a..fdc8198 100644
--- a/node_modules/emdash/src/astro/routes/api/media.ts
+++ b/node_modules/emdash/src/astro/routes/api/media.ts
@@ -103,9 +103,19 @@ export const POST: APIRoute = async ({ request, locals }) => {
 			return apiError("NO_FILE", "No file provided", 400);
 		}
 
+		// Infer MIME type from filename for types browsers don't recognize
+		let fileType = file.type;
+		if (!fileType || fileType === "application/octet-stream") {
+			const ext = file.name.split(".").pop()?.toLowerCase();
+			const mimeMap: Record<string, string> = {
+				woff2: "font/woff2", woff: "font/woff", otf: "font/otf", ttf: "font/ttf",
+			};
+			if (ext && mimeMap[ext]) fileType = mimeMap[ext];
+		}
+
 		// Validate file type
-		const allowedTypes = ["image/", "video/", "audio/", "application/pdf"];
-		if (!allowedTypes.some((type) => file.type.startsWith(type))) {
+		const allowedTypes = ["image/", "video/", "audio/", "application/pdf", "font/", "application/font"];
+		if (!allowedTypes.some((type) => fileType.startsWith(type))) {
 			return apiError("INVALID_TYPE", "File type not allowed", 400);
 		}
 
diff --git a/node_modules/emdash/src/astro/routes/api/media/upload-url.ts b/node_modules/emdash/src/astro/routes/api/media/upload-url.ts
index 13d0c56..48e758b 100644
--- a/node_modules/emdash/src/astro/routes/api/media/upload-url.ts
+++ b/node_modules/emdash/src/astro/routes/api/media/upload-url.ts
@@ -62,9 +62,19 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		const body = await parseBody(request, mediaUploadUrlBody);
 		if (isParseError(body)) return body;
 
+		// Infer MIME type from filename for types browsers don't recognize
+		let contentType = body.contentType;
+		if (!contentType || contentType === "application/octet-stream") {
+			const ext = body.filename.split(".").pop()?.toLowerCase();
+			const mimeMap: Record<string, string> = {
+				woff2: "font/woff2", woff: "font/woff", otf: "font/otf", ttf: "font/ttf",
+			};
+			if (ext && mimeMap[ext]) contentType = mimeMap[ext];
+		}
+
 		// Validate content type
-		const allowedTypes = ["image/", "video/", "audio/", "application/pdf"];
-		if (!allowedTypes.some((type) => body.contentType.startsWith(type))) {
+		const allowedTypes = ["image/", "video/", "audio/", "application/pdf", "font/", "application/font"];
+		if (!allowedTypes.some((type) => contentType.startsWith(type))) {
 			return apiError("INVALID_TYPE", "File type not allowed", 400);
 		}
 
